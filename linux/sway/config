#### Default Sway startup scripts
#### Synchronizes environment variables with systemd and D-Bus 
#### Ensure proper Wayland session integration for user applications and services
include /etc/sway/config.d/*
#### Xwayland is an X server that runs as a Wayland client, translating X11 calls to Wayland
#### so that applications built for X11 can still function even though the system is running on Wayland 
#### Most applications don't need it es. Chromium or Firefox, but it is required by some applications es. Steam or VLC
#### There is no reason to disable it unless for debugging or 'purist' purposes, is a lightweight process
#### But when is enebled invalidate Wayland's security model
#### Xwayland is enabled by default
# xwayland disable
set $mod Mod4
set $alt Mod1
tiling_drag disabled
focus_follows_mouse yes
# My app launcher
set $menu fuzzel
# My terminal emulator
set $term alacritty
# My background image
output * bg /home/m/Dotfiles/Nighthawks_by_Edward_Hopper_1942.jpg fill

###########  
### i/o
###########  
set $laptop_display "Lenovo Group Limited 0x414B Unknown"
set $laptop_keyboard "1:1:AT_Translated_Set_2_keyboard"
# check inputs names with: swaymsg -t get_intputs
# check outputs names with: swaymsg -t get_outputs
# My laptop i/o 
input * xkb_options compose:ralt
input  $laptop_keyboard {
    xkb_options caps:ctrl_modifier,altwin:swap_alt_win
}
# ThinkPad red nipple
input "2:10:TPPS/2_Elan_TrackPoint" {
    pointer_accel -1
}
# 27" external displays
output "Dell Inc. DELL U2725QE GNM6C34" {
    resolution 3840x2160@120.000Hz 
    scale 1.358333
    position 0 0
}
# 27" second vertical external displays
output "LG Electronics LG HDR 4K 0x00006D62" {
    resolution 3840x2160@60.000Hz 
    scale 1.358333
    transform 90
}
# 14" notebook display
output $laptop_display {
    resolution 2880x1800@120.000Hz 
    scale 2
}
# Clamshell mode for laptop
# if the lid is closed whith an external monitor then disable it
bindswitch --reload --locked lid:on output $laptop_display disable
bindswitch --reload --locked lid:off output $laptop_display enable

###########  
### system
###########  
# disable mouse/trakpad es. scrolling on tabbed layout
bindsym button4 nop
bindsym button5 nop
bindsym button6 nop
# start my terminal
bindsym $mod+Return exec $term
# start my launcher
bindsym $mod+space exec $menu
# media key/syms
bindsym XF86AudioRaiseVolume exec wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+ -l 1.2
bindsym XF86AudioLowerVolume exec wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindsym XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle
bindsym XF86AudioMicMute exec pactl set-source-mute @DEFAULT_SOURCE@ toggle
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous
bindsym XF86MonBrightnessDown exec brightnessctl set 5%-
bindsym XF86MonBrightnessUp exec brightnessctl set 5%+
bindsym XF86Display exec swaymsg output "eDP-1" toggle
# save screenshots in the clipboard (secondary selection)
bindsym Print exec grim -g "$(slurp)" - | wl-copy
bindsym control+Print exec grim - | wl-copy
# pick color and save in the clipboard (secondary selection)
bindsym $mod+grave exec hyprpicker | wl-copy
# video with no audio
#bindsym $mod+v exec wf-recorder -g "$(slurp)" -f "$HOME/Videos/screencast-$(date +%Y%m%d-%H%M%S).mp4"
# zen mode
bindsym $mod+z exec /home/m/.local/bin/pomidore zen-toggle

###########  
### workspaces
###########  
# move current workspace to different monitor
bindsym $mod+x move workspace to output right
bindsym $mod+c move workspace to output left
# Allow switching between workspaces with left and right swipes
bindgesture swipe:right workspace prev
bindgesture swipe:left workspace next
# switch to workspace
set $ws_office 5
set $ws_stuff 6
set $ws_main M
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10
bindsym $mod+m workspace $ws_main
# navigate active workspaces
bindsym $mod+comma workspace prev
bindsym $mod+period workspace next
bindsym $mod+Tab workspace back_and_forth


###########  
### containers
###########  
# move focused container to workspace
bindsym $mod+control+1 move container to workspace number 1
bindsym $mod+control+2 move container to workspace number 2
bindsym $mod+control+3 move container to workspace number 3
bindsym $mod+control+4 move container to workspace number 4
bindsym $mod+control+5 move container to workspace number 5
bindsym $mod+control+6 move container to workspace number 6
bindsym $mod+control+7 move container to workspace number 7
bindsym $mod+control+8 move container to workspace number 8
bindsym $mod+control+9 move container to workspace number 9
bindsym $mod+control+0 move container to workspace number 10
bindsym $mod+control+m move container to workspace $ws_main
# change layout mode for the focused container
bindsym $mod+a split horizontal
bindsym $mod+s layout toggle splitv splith
bindsym $mod+d layout toggle stacking tabbed 

###########  
### windows
###########  
# assign window to a workspace with window criteria
# use swaymsg -t get_tree 
assign [app_id="spotify"] $ws_main
assign [app_id="betterbird"] $ws_main
assign [app_id="org.telegram.desktop"] $ws_main
assign [app_id="libreoffice"] $ws_office
assign [app_id="com.transmissionbt*"] $ws_stuff
# floating windows for daemonized programs 
for_window [app_id="org.pulseaudio.pavucontrol"] floating enable
for_window [app_id="blueberry.py"] floating enable
for_window [app_id="org.gnome.Calculator"] floating enable
# resize window 
bindsym $mod+equal resize grow width 10 px or 10 ppt
bindsym $mod+minus resize shrink width 10 px or 10 ppt
bindsym $mod+bracketleft resize shrink height 10 px or 10 ppt
bindsym $mod+bracketright move resize grow height 10 px or 10 ppt
# focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right
# focus parent/child container
bindsym $mod+p focus parent
bindsym $mod+n focus child
# move focused window
bindsym $mod+control+j move left
bindsym $mod+control+k move down
bindsym $mod+control+l move up
bindsym $mod+control+semicolon move right
# kill focused window
bindsym $mod+q kill
# fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle
# floating mode for the focused container
bindsym $mod+r focus mode_toggle
bindsym $mod+e floating toggle
bindsym $mod+w sticky toggle
# reload sway conf
bindsym $mod+control+r reload

bindsym button7 nop  

###########  
### style
###########  
font pango:Ubuntu Nerd Font, bold 10
gaps inner 2
gaps outer 2
smart_gaps on
#2E3440  nord0    black
#3B4252  nord1    dark_gray
#434C5E  nord2    gray
#4C566A  nord3    light_gray
#D8DEE9  nord4    darkest_white
#E5E9F0  nord5    darker_white
#ECEFF4  nord6    white
#8FBCBB  nord7    teal
#88C0D0  nord8    off_blue
#81A1C1  nord9    glacier
#5E81AC  nord10   blue
#BF616A  nord11   red
#D08770  nord12   orange
#EBCB8B  nord13   yellow
#A3BE8C  nord14   green
#B48EAD  nord15   purple
set $black                #2E3440
set $dark_gray            #3B4252
set $gray                 #434C5E
set $light_gray           #4C566A
set $darkest_white        #D8DEE9
set $darker_white         #E5E9F0
set $white                #ECEFF4
set $teal                 #8FBCBB
set $off_blue             #88C0D0
set $glacier              #81A1C1
set $blue                 #5E81AC 
set $red                  #BF616A
set $orange               #D08770
set $yellow               #EBCB8B
set $green                #A3BE8C
set $purple               #B48EAD
#light_graycolor_class    border          backgrount      text            indicator       child_border
client.focused            $darkest_white  $darkest_white  $black          $darkest_white  $darkest_white
client.focused_tab_title  $black          $darkest_white  $black                          
client.focused_inactive   $black          $black          $darkest_white  $darkest_white  $black
client.unfocused          $black          $black          $darkest_white  $darkest_white  $darkest_white
client.urgent             $darkest_white  $darkest_white  $black          $darkest_white  $darkest_white
client.placeholder        $black          $black          $darkest_white  $darkest_white  $darkest_white
client.background         $black                                                          
###########  
### bar
###########  
bar {
    swaybar_command waybar
}

###########  
### apps
###########  
# My notification daemon
exec dunst
# Adjust screen color temperature based on location/time (lat/lon in .config/gammastaep/config.ini)
exec gammastep
# My apps
exec chromium
exec betterbird
exec Telegram
