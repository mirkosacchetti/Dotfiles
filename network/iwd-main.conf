# /etc/iwd/main.conf — iwd standalone: fa da solo DHCP/IP sul Wi-Fi
[General]
EnableNetworkConfiguration=true

[Network]
# pubblica i DNS ricevuti via DHCP in systemd-resolved
NameResolvingService=systemd
